{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd frontend && npm ci && npm run build",
  "installCommand": "echo 'Root install skipped — buildCommand installs inside frontend/'",
  "outputDirectory": "frontend/dist",
  "framework": null
}
